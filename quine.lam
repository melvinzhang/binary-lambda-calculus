/*
cat quine.blc quine.blc
cat quine.blc quine.blc | ./uni -b

quine is a program that outputs input input
quine given its own source code outputs it twice
*/

#define U (\x x x)
#define Q (\self \x \xs \acc \z z x (xs (self self) acc))

\input input (U Q) input
